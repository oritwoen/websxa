name: Release (askweb-opencode)

permissions:
  id-token: write

on:
  push:
    tags:
      - 'askweb-opencode-v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          registry-url: https://registry.npmjs.org
      - run: pnpm i
      - run: pnpm build
      - run: pnpm --filter askweb-opencode build
      - name: Verify tag matches package version
        run: |
          TAG_VERSION="${GITHUB_REF#refs/tags/askweb-opencode-v}"
          PKG_VERSION=$(node -p "require('./packages/askweb-opencode/package.json').version")
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "Tag version ($TAG_VERSION) doesn't match package.json ($PKG_VERSION)"
            exit 1
          fi
      - run: pnpm --filter askweb-opencode publish --access public --no-git-checks --provenance
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
